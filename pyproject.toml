[project]
name = "pdfmerger"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pypdf>=6.0.0",
]

[dependency-groups]
install = [
    "pyinstaller>=6.16.0",
]
license = [
    "pip-licenses>=5.0.0",
]
